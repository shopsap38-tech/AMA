<IfModule mod_rewrite.c>
    RewriteEngine On
    # Si l'application est déployée à la racine du vhost, on redirige tout vers /public.
    RewriteCond %{REQUEST_URI} !^/public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
